# Suggested Commands

No project-specific dev/test/lint/build/run commands yet (no stack). Add them once a package manifest / task runner is introduced.

Darwin note: system utils are BSD variants (`sed`, `date`, `stat`, `find` differ from GNU). Prefer `git` for file listing (`git ls-files`) and avoid GNU-only flags.
