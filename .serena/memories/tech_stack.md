# Tech Stack

Not yet established — no package manifest, lockfile, or source files exist. Serena detected no programming language.

Populate on first commit that introduces a stack (language, framework, package manager, build tool, version pins). Until then, do not assume a stack from the project name.
