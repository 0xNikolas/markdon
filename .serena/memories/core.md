# Core

Greenfield project `markdon` at `/Users/nicu/Projects/markdon`. As of onboarding, effectively empty: single git commit (`first commit`), only tracked file is `README.md` (contains just the title `# markdon`). No source code, build config, or package manifest yet.

Name suggests a Markdown-related tool (unconfirmed — no code to verify intent). Confirm actual purpose/direction with the user before assuming.

Domain memories (currently stubs pending first code — update when the stack lands):
- Stack, build tools, package manager: `mem:tech_stack`
- Commands the user runs: `mem:suggested_commands`
- Code style / naming / patterns: `mem:conventions`
- Definition-of-done commands: `mem:task_completion`

Platform: Darwin (macOS), zsh.
